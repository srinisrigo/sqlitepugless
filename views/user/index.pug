extends ../layout

block content
  h1= title
  p Welcome to #{title}
  if users
    table
        each user in users
            tr
                td= user.name 
                td= user.email
                td 
                  a(href=`user/${user.id}`) show
